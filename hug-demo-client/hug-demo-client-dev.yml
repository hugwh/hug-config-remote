server:
  port: 8002
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/
spring:
  application:
    name: hug-demo-client
  cloud:
    config:
      uri: http://localhost:8006/
      profile: dev
      label: master
feign:
  hystrix:
    enabled: true